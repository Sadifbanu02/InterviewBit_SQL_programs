SELECT
COUNT(Name)
FROM EMPLOYEE E INNER JOIN EVALUATION V
ON E.Points 
BETWEEN V.Lower 
AND V.Upper 
WHERE V.Rating < 3;

SELECT COUNT(Name) 
FROM EMPLOYEE E INNER JOIN EVALUATION V
ON E.Points BETWEEN V.Lower 
AND V.Upper 
WHERE V.Rating > 8;
